<div class="container mt-4">
  <div class="row mb-4">
    <div class="col-md-8">
      <h1>📦 Prodotti (SKU)</h1>
      <p class="text-muted">Configura il routing dei prodotti verso i flussi di stampa</p>
    </div>
    <div class="col-md-4 text-end">
      <a href="/products/export" class="btn btn-info">📤 Esporta CSV</a>
      <a href="/products/import" class="btn btn-success">📥 Importa da CSV</a>
      <a href="/products/new" class="btn btn-primary">➕ Nuovo Prodotto</a>
    </div>
  </div>

  <% if params[:success] %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      ✓ Prodotto <%= params[:success] %> con successo!
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
  <% end %>

  <!-- Search Form -->
  <div class="row mb-3">
    <div class="col-md-12">
      <form method="get" action="/products" class="d-flex gap-2">
        <input type="text" name="search" class="form-control" placeholder="Cerca per SKU, nome prodotto o categoria..." value="<%= @search_term %>">
        <button type="submit" class="btn btn-outline-secondary">🔍 Cerca</button>
        <% if @search_term.present? %>
          <a href="/products" class="btn btn-outline-secondary">✕ Azzera</a>
        <% end %>
      </form>
    </div>
  </div>

  <% if @search_term.present? %>
    <div class="alert alert-info alert-sm">
      Risultati per: <strong><%= @search_term %></strong> (<%= @products.count %> prodotto<%= @products.count != 1 ? 'i' : '' %>)
    </div>
  <% end %>

  <% if @products.any? %>
    <div class="table-responsive">
      <table class="table table-striped table-hover">
        <thead class="table-dark">
          <tr>
            <th>SKU</th>
            <th>Nome Prodotto</th>
            <th>Categoria</th>
            <th>Flusso</th>
            <th>Note</th>
            <th>Stato</th>
            <th>Azioni</th>
          </tr>
        </thead>
        <tbody>
          <% @products.each do |product| %>
            <tr>
              <td>
                <strong><code><%= product.sku %></code></strong>
              </td>
              <td>
                <%= product.name %>
              </td>
              <td>
                <% if product.product_category %>
                  <span class="badge bg-secondary"><%= product.product_category.name %></span>
                <% else %>
                  <span class="text-muted">—</span>
                <% end %>
              </td>
              <td>
                <% if product.default_print_flow %>
                  <span class="badge bg-info"><%= product.default_print_flow.name %></span>
                <% else %>
                  <span class="badge bg-warning">Non assegnato</span>
                <% end %>
              </td>
              <td>
                <small><%= product.notes.present? ? product.notes : '—' %></small>
              </td>
              <td>
                <% if product.active %>
                  <span class="badge bg-success">✓ Attivo</span>
                <% else %>
                  <span class="badge bg-danger">✗ Inattivo</span>
                <% end %>
              </td>
              <td>
                <a href="/products/<%= product.id %>/edit" class="btn btn-sm btn-outline-primary">✎ Modifica</a>
                <button class="btn btn-sm btn-outline-info" onclick="duplicateProduct(<%= product.id %>)">📋 Duplica</button>
                <button class="btn btn-sm btn-outline-danger" onclick="deleteProduct(<%= product.id %>)">🗑 Elimina</button>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="alert alert-info">
      📭 Nessun prodotto registrato. <a href="/products/new">Crea il primo prodotto</a>
    </div>
  <% end %>
</div>

<script>
function duplicateProduct(id) {
  fetch('/products/' + id + '/duplicate', { method: 'POST' })
    .then(res => res.json())
    .then(data => {
      if (data.success) {
        location.href = '/products?success=duplicated';
      } else {
        alert('Errore: ' + (data.error || 'Duplicazione fallita'));
      }
    })
    .catch(err => alert('Errore: ' + err));
}

function deleteProduct(id) {
  if (confirm('Sei sicuro? Eliminerai questo prodotto dal routing.')) {
    fetch('/products/' + id, { method: 'DELETE' })
      .then(() => location.href = '/products?success=deleted');
  }
}
</script>
