# Backup Configuration - Add these to your .env file
# Backup configuration for sending to Enfocus Switch server

# Remote server where backups will be stored
REMOTE_BACKUP_USER=orchestrator
REMOTE_BACKUP_HOST=192.168.1.162
REMOTE_BACKUP_PATH=/backup/print-orchestrator

# SSH private key path for passwordless backup transfer
# Generate with: ssh-keygen -t rsa -b 4096 -f ~/.ssh/backup_key -N ""
REMOTE_BACKUP_SSH_KEY=/home/paolo/.ssh/backup_key
