<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Print Order Orchestrator</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
  <style>
    :root {
      --form-element-spacing-vertical: 0.5rem;
      --form-element-spacing-horizontal: 1rem;
    }
    body {
      margin: 0;
    }
    nav {
      padding: 1rem 0;
      border-bottom: 1px solid var(--muted-border-color);
      margin-bottom: 2rem;
    }
    nav ul {
      margin: 0;
      display: flex;
      gap: 2rem;
      list-style: none;
      padding: 0;
    }
    nav a {
      text-decoration: none;
    }
    nav .brand {
      font-weight: bold;
      font-size: 1.2rem;
      margin-right: 2rem;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }
    .status-badge {
      font-size: 0.85em;
      padding: 0.35em 0.65em;
      border-radius: 0.3rem;
    }
    .status-new { background-color: #0d6efd; color: white; }
    .status-sent_to_switch { background-color: #6c757d; color: white; }
    .status-processing { background-color: #ffc107; color: #000; }
    .status-done { background-color: #198754; color: white; }
    .status-error { background-color: #dc3545; color: white; }
    .asset-preview {
      max-width: 100px;
      max-height: 100px;
      object-fit: cover;
      border: 1px solid var(--muted-border-color);
      border-radius: 0.5rem;
    }
    .log-box {
      background-color: var(--form-element-background-color);
      border: 1px solid var(--muted-border-color);
      border-radius: 0.5rem;
      padding: 1rem;
      font-family: monospace;
      font-size: 0.85rem;
      white-space: pre-wrap;
      max-height: 300px;
      overflow-y: auto;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    table thead th {
      background-color: var(--muted-background-color);
      padding: 1rem;
      text-align: left;
      font-weight: 600;
    }
    table tbody td {
      padding: 1rem;
      border-bottom: 1px solid var(--muted-border-color);
    }
    table tbody tr:hover {
      background-color: var(--muted-background-color);
    }
    .btn-group {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }
    .btn-small {
      padding: 0.5rem 1rem;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <nav>
    <div class="container">
      <ul>
        <li><a href="/" class="brand">🖨️ Print Orchestrator</a></li>
        <li><a href="/orders">📋 Ordini</a></li>
        <li><a href="/line_items">🔗 Lavorazioni</a></li>
        <li><a href="/inventory">📦 Magazzino</a></li>
        <li><details style="margin: 0;">
          <summary>⚙️ Impostazioni</summary>
          <ul style="position: absolute; top: 100%; left: 0; background: white; border: 1px solid var(--muted-border-color); border-radius: 0.5rem; padding: 0.5rem; min-width: 200px;">
            <li><a href="/stores">🏪 Negozi</a></li>
            <li><a href="/print_flows">🔄 Flussi di Stampa</a></li>
            <li><a href="/admin/print_machines">🔧 Macchine da Stampa</a></li>
            <li><a href="/webhooks">🔗 Webhook Switch</a></li>
            <li style="border-top: 1px solid var(--muted-border-color); margin: 0.5rem 0;"></li>
            <li><a href="/products">📦 Prodotti</a></li>
            <li><a href="/product_categories">📂 Categorie</a></li>
            <li style="border-top: 1px solid var(--muted-border-color); margin: 0.5rem 0;"></li>
            <li><a href="/admin/cleanup">🧹 Storage Cleanup</a></li>
          </ul>
        </details></li>
      </ul>
    </div>
  </nav>
  
  <div class="container">
    <%= yield %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@picocss/pico@2/js/pico.min.js"></script>
</body>
</html>
