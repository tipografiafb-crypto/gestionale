<div class="flex justify-between items-start mb-6">
  <div>
    <h1 class="text-3xl font-bold">🔗 Switch Webhooks</h1>
    <p class="text-gray-600 text-sm mt-1">Registra i tuoi endpoint Switch qui</p>
  </div>
  <a href="/webhooks/new" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-semibold transition">➕ Nuovo Webhook</a>
</div>

<% if params[:success] %>
  <div class="bg-green-50 border border-green-200 rounded-lg p-4 text-green-700 mb-4">
    ✓ Webhook <%= params[:success] %> con successo!
  </div>
<% end %>

<% if @webhooks.any? %>
  <div class="overflow-x-auto rounded-lg border border-gray-200 shadow-sm">
    <table class="w-full">
      <thead>
        <tr class="bg-gray-800 text-white border-b">
          <th class="px-4 py-3 text-left text-sm font-semibold">Nome</th>
          <th class="px-4 py-3 text-left text-sm font-semibold">Hook Path</th>
          <th class="px-4 py-3 text-left text-sm font-semibold">URL Completo</th>
          <th class="px-4 py-3 text-left text-sm font-semibold">Negozio</th>
          <th class="px-4 py-3 text-left text-sm font-semibold">Azioni</th>
        </tr>
      </thead>
      <tbody>
        <% @webhooks.each do |webhook| %>
          <tr class="border-b border-gray-200 hover:bg-gray-50 transition">
            <td class="px-4 py-3"><strong><%= webhook.name %></strong></td>
            <td class="px-4 py-3"><code class="text-xs bg-gray-100 px-2 py-1 rounded"><%= webhook.hook_path %></code></td>
            <td class="px-4 py-3"><code class="text-xs bg-gray-100 px-2 py-1 rounded break-all"><%= webhook.webhook_url %></code></td>
            <td class="px-4 py-3">
              <% if webhook.store %>
                <span class="bg-blue-100 text-blue-700 text-xs font-semibold px-2 py-0.5 rounded-full"><%= webhook.store.name %></span>
              <% else %>
                <span class="bg-gray-200 text-gray-700 text-xs font-semibold px-2 py-0.5 rounded">Globale</span>
              <% end %>
            </td>
            <td class="px-4 py-3 flex gap-2">
              <a href="/webhooks/<%= webhook.id %>/edit" class="bg-blue-100 hover:bg-blue-200 text-blue-700 px-2 py-1 rounded text-xs font-semibold transition">✎ Modifica</a>
              <button class="bg-red-100 hover:bg-red-200 text-red-700 px-2 py-1 rounded text-xs font-semibold transition" onclick="deleteWebhook(<%= webhook.id %>)">🗑 Elimina</button>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 text-blue-700">
    📭 Nessun webhook registrato. <a href="/webhooks/new" class="font-semibold hover:underline">Crea il primo webhook</a>
  </div>
<% end %>

<script>
function deleteWebhook(id) {
  if (confirm('Sei sicuro? Eliminerai questo webhook.')) {
    fetch('/webhooks/' + id, { method: 'DELETE' })
      .then(() => location.href = '/webhooks?success=deleted');
  }
}
</script>
