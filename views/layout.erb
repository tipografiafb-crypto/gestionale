<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Print Order Orchestrator</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .sidebar { transition: margin-left 0.3s ease; }
    .sidebar-toggle:checked ~ .sidebar { margin-left: 0; }
    .sidebar-toggle:checked ~ .main-content { margin-left: 0; }
    .status-new { @apply bg-blue-100 text-blue-800; }
    .status-sent_to_switch { @apply bg-gray-100 text-gray-800; }
    .status-processing { @apply bg-yellow-100 text-yellow-800; }
    .status-done { @apply bg-green-100 text-green-800; }
    .status-error { @apply bg-red-100 text-red-800; }
    .asset-preview { @apply max-w-[100px] max-h-[100px] object-cover border border-gray-300 rounded; }
    .log-box { @apply bg-gray-50 border border-gray-300 rounded p-4 font-mono text-sm whitespace-pre-wrap max-h-80 overflow-y-auto; }
  </style>
</head>
<body class="bg-gray-50">
  <div class="flex h-screen">
    <!-- Sidebar -->
    <div class="w-64 bg-white shadow-lg border-r border-gray-200 sidebar overflow-y-auto">
      <div class="p-6 border-b border-gray-200 sticky top-0 bg-white">
        <div class="flex items-center justify-between">
          <h1 class="text-2xl font-bold text-blue-600 flex items-center gap-2">
            <span>🖨️</span>
            <span class="text-lg">Orchestrator</span>
          </h1>
          <button id="close-sidebar" class="lg:hidden text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
          </button>
        </div>
      </div>
      
      <nav class="p-4 space-y-1">
        <a href="/orders" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">📋 Ordini</a>
        <a href="/line_items" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">🔗 Lavorazioni</a>
        <a href="/inventory" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">📦 Magazzino</a>
        
        <div class="mt-6 pt-4 border-t border-gray-200">
          <p class="px-4 text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">Impostazioni</p>
          <a href="/stores" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">🏪 Negozi</a>
          <a href="/print_flows" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">🔄 Flussi di Stampa</a>
          <a href="/admin/print_machines" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">🔧 Macchine</a>
          <a href="/webhooks" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">🔗 Webhook Switch</a>
          <a href="/products" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">📦 Prodotti</a>
          <a href="/product_categories" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">📂 Categorie</a>
          <a href="/admin/cleanup" class="block px-4 py-2 rounded-lg text-gray-700 hover:bg-blue-50 transition">🧹 Storage</a>
        </div>
      </nav>
    </div>

    <!-- Main Content -->
    <div class="flex-1 flex flex-col">
      <!-- Top Header -->
      <div class="bg-white border-b border-gray-200 shadow-sm sticky top-0 z-10">
        <div class="px-8 py-4 flex items-center justify-between">
          <div>
            <h2 class="text-xl font-semibold text-gray-900">Print Order Orchestrator</h2>
            <p class="text-sm text-gray-500">Gestione ordini e flussi di stampa</p>
          </div>
          <button id="toggle-sidebar" class="lg:hidden p-2 hover:bg-gray-100 rounded-lg">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
          </button>
        </div>
      </div>

      <!-- Content -->
      <div class="flex-1 overflow-auto">
        <div class="p-8">
          <%= yield %>
        </div>
      </div>
    </div>
  </div>

  <script>
    const toggleBtn = document.getElementById('toggle-sidebar');
    const closeBtn = document.getElementById('close-sidebar');
    const sidebar = document.querySelector('.sidebar');
    
    toggleBtn?.addEventListener('click', () => {
      sidebar.classList.toggle('-translate-x-full');
    });
    
    closeBtn?.addEventListener('click', () => {
      sidebar.classList.add('-translate-x-full');
    });
  </script>
</body>
</html>
